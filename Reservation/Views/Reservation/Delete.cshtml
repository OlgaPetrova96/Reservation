@using Reservation.Models
@model Reservation

@{
    ViewData["Title"] = "Удалить?";
}

<h3>Удалить?</h3>
<div>
    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Удалить" class="btn btn-default hov" />
        <button class="btn btn-default hov" asp-action="Index">Отмена</button>
    </form>
</div>